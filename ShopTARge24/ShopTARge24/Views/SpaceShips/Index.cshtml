@model IEnumerable<ShopTARge24.Models.SpaceShips.SpaceShipsIndexViewModel>

<div>
	<a asp-action="create" id="testIdCreate" asp-controller="Spaceships" type="submit" class="btn btn-primary">Create</a>
</div>

<div class="h1">
	Spaceships
</div>
<form method="post">
	<table class="table table-hover">
		<thead>
			<tr>
				<th scope="col">Nr</th>
				<th scope="col">Name</th>
				<th scope="col">Type</th>
				<th scope="col">Build date</th>
				<th scope="col">Crew</th>
				<th scope="col">Modify</th>
			</tr>
		</thead>
		<tbody>
			@{
				int rowNumber = 0;
			}
			@foreach (var spaceship in Model)
			{
				<tr>
					<td id="testId">@{rowNumber++;} @rowNumber</td>
					<td id="testIdName">@spaceship.Name</td>
					<td id="testIdCal">@spaceship.Classification</td>
					<td id="testIdBuild">@spaceship.BuildDate</td>
					<td id="testIdCrew">@spaceship.Crew</td>
					<td>
					<a asp-action="details" id="details" asp-controller="spaceships" asp-route-id="@spaceship.Id" class="btn btn-outline-primary">Details</a>
					|
						<a asp-action="update" id="update" asp-controller="spaceships" asp-route-id="@spaceship.Id" class="btn btn-outline-warning">Update</a>
					   |
						<a asp-action="delete" id="delete" asp-controller="spaceships" 	asp-route-id="@spaceship.Id" class="btn btn-outline-danger">Delete</a>
						
				</td>
				</tr>	
			}
		</tbody>
	</table>
</form>
